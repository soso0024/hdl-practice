/**
 * 16-bit bitwise Or gate: for i=0..15 out[i] = a[i] or b[i].
 */

CHIP Or16 {
    IN a[16], b[16];
    OUT out[16];

    PARTS:
    Nand16(a=a, b=a, out=aNanda);
    Nand16(a=b, b=b, out=bNandb);
    Nand16(a=aNanda, b=bNandb, out=out);
}