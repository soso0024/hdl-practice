/**
 * Multiplexor. If sel==1 then out=b else out=a.
 */

CHIP Mux {
    IN a, b, sel;
    OUT out;

    PARTS:
    // NOT sel
    Nand(a=sel, b=sel, out=notSel);

    // a AND NOT sel
    Nand(a=a, b=notSel, out=w1);

    // b AND sel
    Nand(a=b, b=sel, out=w2);

    // Combine results
    Nand(a=w1, b=w2, out=out);
}

// ref: https://vlsiuniverse.blogspot.com/2016/11/2x1-mux-using-nand-gates.html